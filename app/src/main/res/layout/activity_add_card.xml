<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data />
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:fillViewport="true">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingBottom="16dp">

            <TextView android:id="@+id/backButton" style="@style/Widget.Wallet.BackButton" />
            <TextView
                style="@style/TextAppearance.Wallet.Display"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/add_card_title" />

            <TextView style="@style/TextAppearance.Wallet.Section" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="28dp" android:text="@string/card_id" />
            <EditText android:id="@+id/cardIdInput" style="@style/Widget.Wallet.Input" android:hint="@string/card_id_hint" android:imeOptions="actionNext" android:inputType="textCapCharacters" />

            <TextView style="@style/TextAppearance.Wallet.Section" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="16dp" android:text="@string/card_name" />
            <EditText android:id="@+id/cardNameInput" style="@style/Widget.Wallet.Input" android:hint="@string/card_name_hint" android:imeOptions="actionNext" android:inputType="textPersonName|textCapWords" />

            <TextView style="@style/TextAppearance.Wallet.Section" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="16dp" android:text="@string/card_number" />
            <EditText android:id="@+id/cardNumberInput" style="@style/Widget.Wallet.Input" android:digits="0123456789 " android:hint="@string/card_number_hint" android:imeOptions="actionNext" android:inputType="phone" android:maxLength="19" />

            <TextView style="@style/TextAppearance.Wallet.Section" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="16dp" android:text="@string/initial_balance" />
            <EditText android:id="@+id/balanceInput" style="@style/Widget.Wallet.Input" android:hint="@string/amount_input_hint" android:imeOptions="actionDone" android:inputType="numberDecimal" />

            <Space android:layout_width="1dp" android:layout_height="28dp" />
            <com.google.android.material.button.MaterialButton
                android:id="@+id/addCardButton"
                style="@style/Widget.Wallet.PrimaryButton"
                android:layout_width="match_parent"
                android:text="@string/add_card_action" />
        </LinearLayout>
    </ScrollView>
</layout>
